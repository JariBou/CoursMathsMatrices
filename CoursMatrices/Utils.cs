namespace CoursMatrices;

public static class Utils
{
    public static class Math
    {
        public const float Deg2Rad = MathF.PI * 2 / 360 ;
        public const float Rad2Deg = 360f / (MathF.PI * 2);
    }
}